# SQL-restaurant-db
This repository contains SQL practice queries using  restaurant database.  
The project focuses on core SQL concepts such as SELECT, WHERE, AND, aggregation functions (COUNT, MIN, MAX, AVG), GROUP BY, and subqueries.  
All queries are written and executed using MySQL and MySQL Workbench. 
